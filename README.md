# locatoAi

## Backend runtime

The Python backend targets Python 3.8.10. From `backend/`, create the
virtualenv with that interpreter before installing dependencies:

```bash
python3.8 -m venv .venv
.venv/bin/pip install -e ".[dev]"
```
