"""Agent call 1: layer selection — Day 2.

Input: user query + catalog metadata (name/description/tags only —
provider metadata is untrusted; descriptions truncated before prompting).
Output: list of layer ids, or a clarify question.

The prompt is ported unchanged from the demo into prompts/.
"""

# DAY 2: implement select_layers(query, catalog) here.
