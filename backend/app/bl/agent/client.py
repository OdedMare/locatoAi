"""Anthropic client wrapper — Day 2.

Will provide:
- env-key client construction (`anthropic` SDK)
- a JSON-mode helper that strips markdown fences and retries once on
  invalid JSON
"""

# DAY 2: implement AnthropicClient here. Nothing in Day 1 imports this.
