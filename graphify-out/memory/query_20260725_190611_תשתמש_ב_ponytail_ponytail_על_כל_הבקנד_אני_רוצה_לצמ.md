---
type: "query"
date: "2026-07-25T19:06:11.060068+00:00"
question: "תשתמש ב ponytail:ponytail על כל הבקנד אני רוצה לצמצם קוד מיותר ועטיפות מיותר"
contributor: "graphify"
outcome: "useful"
source_nodes: ["ApplicationStateWiring", "GeoUtils", "CatalogRouter", "Provider registry: catalog `provider` name → adapter instance.  OCP: registering"]
---

# Q: תשתמש ב ponytail:ponytail על כל הבקנד אני רוצה לצמצם קוד מיותר ועטיפות מיותר

## Answer

Expanded from original query via graph vocabulary: [wrapper, helper, duplicate, factory, adapter, protocol, registry, mapper, builder, loader, parser, utils]. Verified against current callers, then removed stateless namespace classes and pass-through aliases across composition, helpers, LLM utilities, and all service routers while preserving provider ports, validation, error handling, and HTTP contracts. Full backend suite: 316 passed using a temporary flunks compatibility stub because the private dependency is unavailable from the configured package index.

## Outcome

- Signal: useful

## Source Nodes

- ApplicationStateWiring
- GeoUtils
- CatalogRouter
- Provider registry: catalog `provider` name → adapter instance.  OCP: registering