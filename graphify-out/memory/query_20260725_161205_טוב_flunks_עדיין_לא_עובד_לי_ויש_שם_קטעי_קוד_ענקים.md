---
type: "query"
date: "2026-07-25T16:12:05.071910+00:00"
question: "טוב flunks עדיין לא עובד לי ויש שם קטעי קוד ענקים שקשה לדבג צמצם אותם רק לחלקים הרלוונטים; אני מקבל שגיאה is_partial_success should be a valid str; flunks בסוף מחזיר dataframe"
contributor: "graphify"
outcome: "useful"
source_nodes: ["FlowPackageProvider", "FlapiProvider"]
---

# Q: טוב flunks עדיין לא עובד לי ויש שם קטעי קוד ענקים שקשה לדבג צמצם אותם רק לחלקים הרלוונטים; אני מקבל שגיאה is_partial_success should be a valid str; flunks בסוף מחזיר dataframe

## Answer

Expanded from original query via graph vocab: [flapi, flow, gateway, metadata, package, partial, patch, provider, results, schema, success]. The FLAPI path is FlowPackageProvider -> FlowPackageGateway -> FlunksRunner -> DataFrame normalization. The isPartialSuccess failure persisted because Pydantic rebuilt FlowResults before MetaData and because some flunks builds expose is_partial_success under an isPartialSuccess alias. The patch now finds either form, preserves Optional[str], rebuilds MetaData before FlowResults, requires the final DataFrame contract, compacts validation errors, and restores missing temporal-field inference. All FLAPI functions are at most 20 lines; 302 Python 3.8 tests passed with a faithful flunks stub. A real Docker build remains blocked because the internal flunks distribution is not configured in the container pip index.

## Outcome

- Signal: useful

## Source Nodes

- FlowPackageProvider
- FlapiProvider